{
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "nodemon --watch src --watch styles --ext html,mjs,css --exec \"node scripts/build.mjs\"",
    "export": "node scripts/build.mjs && node scripts/export-pdf.mjs",
    "banner": "node scripts/export-banner.mjs",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "playwright": "^1.60.0"
  }
}
